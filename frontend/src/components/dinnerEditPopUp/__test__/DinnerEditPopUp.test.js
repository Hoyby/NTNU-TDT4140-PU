import React from 'react'
import { cleanup, screen, render } from '@testing-library/react'
import userEvent from '@testing-library/user-event'

describe("DinnerEditPopUp", () => {

    afterEach(cleanup)

    test("Renders without crashing", () => {
        
    })
})
